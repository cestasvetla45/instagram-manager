import ReelGallery from "../components/ReelGallery";

export default function InspirationPage() {
  return (
    <ReelGallery
      type="inspiration"
      title="Inspiration Reels"
      subtitle="Your saved reference reels. Search, sort by performance, download, or refresh live metrics."
    />
  );
}
